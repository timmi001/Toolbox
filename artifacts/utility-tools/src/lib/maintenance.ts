// Central maintenance flags for feature gating
// Flip `VIDEO_DOWNLOAD_MAINTENANCE` to `false` to re-enable downloaders.
export const VIDEO_DOWNLOAD_MAINTENANCE = true;

export default {
  VIDEO_DOWNLOAD_MAINTENANCE,
};
