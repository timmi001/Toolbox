{
  "buildCommand": "pnpm --filter @workspace/utility-tools run build",
  "outputDirectory": "artifacts/utility-tools/dist/public",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
